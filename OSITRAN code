#%%
# Parameters
inicio = "112025"
final = "072026"

#%%
# # Parameters
# inicio = "012025"
# final = "012025"
#%%
import pandas as pd
import numpy as np
import re
import datetime
import os
#%% md
*Correr solo desde enero 2019*
#%%
# inicio = '112024'
# final = '112024'
#%%
# mes = 'Febrero'
# anio = '2023'
# mes_ = "fe"
# meses_n = "02"
# meses_ = "fe"
#mes_cod="Feb."
#%%
a1, a2, m1, m2 = inicio[2::], final[2::], inicio[0:2], final[0:2]
rango_anios = range(int(a1),int(a2)+1)
#%%
meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto', 'Setiembre', 'Octubre', 'Noviembre', 'Diciembre']
meses_ = ['en','fe','ma','ab','my','jn','jl','ag','se','oc','no','di']
meses_n = ['01','02','03','04','05','06','07','08','09','10','11','12']
meses_m = ['enero', 'febrero', 'marzo', 'abril', 'mayo', 'junio', 'julio', 'agosto', 'setiembre', 'octubre', 'noviembre', 'diciembre']
meses_cod=['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Setiembre','Octubre','Noviembre','Diciembre']
meses_cod2=['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Septiembre','Octubre','Noviembre','Diciembre']
#%%
### Eliminando todos exceles de la carpeta
#%%
# folder_path = r'Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\BD\OSITRÁN'

# # Verifica si la carpeta existe
# if not os.path.exists(folder_path):
#     print(f'La carpeta {folder_path} no existe.')
# else:
#     # Recorre todos los archivos en la carpeta
#     for filename in os.listdir(folder_path):
#         # Comprueba si el archivo tiene la extensión .xlsx o .csv
#         if filename.endswith('.xlsx') or filename.endswith('.csv'):
#             # Construye la ruta completa del archivo
#             file_path = os.path.join(folder_path, filename)
#             try:
#                 # Elimina el archivo
#                 os.remove(file_path)
#                 print(f'Archivo eliminado: {file_path}')
#             except Exception as e:
#                 print(f'No se pudo eliminar el archivo {file_path}. Error: {e}')

#     print('Proceso completado.')
#%%
### Creación de backups
#%%
import os
import shutil

#Archivo
source_file = r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\Históricos\Ositran.xlsx"
#landing
destination_dir = r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\Históricos\Backup"

def copy_file_to_directory(source, des):
    # Crear el directorio de destino si no existe
    if not os.path.exists(des):
        os.makedirs(des)
    # Extraer el nombre del archivo de la ruta de origen
    file_name = os.path.basename(source)
    # Construir la ruta de destino completa
    dest_file = os.path.join(des, file_name)
    try:
        # Copiar el archivo
        shutil.copyfile(source, dest_file)
        print(f"Archivo copiado exitosamente.")
    except Exception as e:
        print(f"Error al copiar el archivo: {str(e)}")

copy_file_to_directory(source_file, destination_dir)
#%%
array_rutas = []
for i in rango_anios:
    if(rango_anios[-1]==i):
        if (len(rango_anios)==1):
            for j in range(int(m1),int(m2)+1):
                if(len(str(j))==1):
                    j = '0'+ str(j)
                    
                ruta = str(j) + str(i)
                array_rutas.append(ruta)

        else:
            for j in range(1,int(m2)+1):
                if(len(str(j))==1):
                    j = '0' + str(j)
                ruta = str(j) + str(i) 
                array_rutas.append(ruta)
                
    elif(i != rango_anios[0]):
        for j in range(1,13):
            if(len(str(j))==1):
                j = '0' + str(j)
            ruta = str(j) + str(i)
            array_rutas.append(ruta)
        
    else:
        if (len(rango_anios)==1):
            for j in range(int(m1),int(m2)+1):
                if(len(str(j))==1):
                    j = '0' + str(j)
                ruta = str(j) + str(i)
                array_rutas.append(ruta)

        else:
            for j in range(int(m1),13):
                if(len(str(j))==1):
                    j = '0' + str(j)
                ruta = str(j) + str(i)
                array_rutas.append(ruta)
#%%
archivo1= r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\BD\OSITRÁN\\Carreteras.xlsx"
archivo2= r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\BD\OSITRÁN\\Ferrovias.xlsx"

#%%
def carretera(empresa,base,titulo,nombres):    
    
    df1_ = pd.read_excel(archivo1,base,header=None)
    df1_=df1_.iloc[4:,]
    df1_=df1_.reset_index().iloc[::,1:]

    if titulo == "C) Ingresos":

        primerafila=df1_[df1_[1]==titulo].index[0]+1
        df1_=df1_.iloc[primerafila:primerafila+5,]

    else:
        primerafila=df1_[df1_[1]==titulo].index[0]+1
        df1_=df1_.iloc[primerafila:]
        df1_=df1_.reset_index().drop("index",axis=1)
        ultimafila=df1_[df1_[1]=='TOTAL'].index[0]+3
        df1_=df1_.iloc[:ultimafila]
        df1_ = pd.concat([df1_.head(2), df1_.tail(3)])
        df1_=df1_.reset_index().drop("index",axis=1)

    primcol = df1_.columns.get_loc(df1_.columns[df1_.iloc[0] == int(anio)][0])
    df1_=df1_.iloc[1:,primcol:primcol+13]
    df1_=df1_.reset_index().drop("index",axis=1)

    df1_=df1_.transpose()
    df1_ = df1_.loc[df1_[0] == mes_cod]
    df1_=df1_.transpose().iloc[1:]

    df1_.insert(0,'Tipo',nombres)
    df1_.insert(0,'Categoría',titulo)
    df1_=df1_.set_axis(['Categoría','Tipo','Valor'],axis=1)
    df1_.insert(0,"Empresa",empresa)
    
    return df1_
#%%
def ferrovias(empresa,titulo):    

    df1_ = pd.read_excel(archivo2,sheet_name='3. Línea 1',header=None)

    df1_=df1_.iloc[4:,1:]
    df1_=df1_.reset_index().iloc[::,1:]

    primerafila=df1_[df1_[1]==titulo].index[0]+1
    df1_=df1_.iloc[primerafila:]
    df1_=df1_.reset_index().drop("index",axis=1)

    empty_indices = df1_[1][df1_[1].isna()].index

    if len(empty_indices) > 1:
        ultimafila = empty_indices[1]
    else:
        ultimafila = None

    df1_=df1_.iloc[:ultimafila]
    nombres=df1_[1][2:].to_list()

    primcol = df1_.columns.get_loc(df1_.columns[df1_.iloc[0] == int(anio)][0])
    df1_=df1_.iloc[1:,primcol:primcol+13]
    df1_=df1_.reset_index().drop("index",axis=1)

    df1_=df1_.transpose()
    df1_ = df1_.loc[df1_[0] == mes_cod2]
    df1_=df1_.transpose().iloc[1:]

    df1_.insert(0,'Tipo',nombres)

    df1_.insert(0,'Categoría',titulo)
    df1_=df1_.set_axis(['Categoría','Tipo','Valor'],axis=1)
    df1_.insert(0,"Empresa",empresa)
    
    return df1_
#%%
def main(mes, anio):
    
    archivo1= r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\BD\OSITRÁN\\Carreteras.xlsx"
    archivo2= r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\BD\OSITRÁN\\Ferrovias.xlsx"

    File_found = False

    for i in range (12,0, -1):
        if os.path.isfile(archivo1):
            File_found=True
            last_file = archivo1
            break
    if not File_found:
        print("Error Base: "+ mes + " " +anio)

    for i in range (12,0, -1):
        if os.path.isfile(archivo2):
            File_found=True
            last_file = archivo2
            break
    if not File_found:
        print("Error Base: "+ mes + " " +anio)

    #Carreteras:

    nombres_unidad=['Total','Ligero','Pesado']
    nombres_ingreso=['Total de Recaudación','Livianos','Pesados']

    Canchaque_unidad=carretera('Canchaque','2. CANCHAQUE','A) Tráfico en unidades - Total',nombres_unidad)
    Canchaque_eje=carretera('Canchaque','2. CANCHAQUE','B) Tráfico en ejes - Total',nombres_unidad)
    Canchaque_ingreso=carretera('Canchaque','2. CANCHAQUE','C) Ingresos',nombres_ingreso)

    Norvial_unidad=carretera('Norvial','6. NORVIAL','A) Tráfico en unidades - Total',nombres_unidad)
    Norvial_eje=carretera('Norvial','6. NORVIAL','B) Tráfico en ejes - Total',nombres_unidad)
    Norvial_ingreso=carretera('Norvial','6. NORVIAL','C) Ingresos',nombres_ingreso)

    Survial_unidad=carretera('Survial','10. SURVIAL','A) Tráfico en unidades - Total',nombres_unidad)
    Survial_eje=carretera('Survial','10. SURVIAL','B) Tráfico en ejes - Total',nombres_unidad)
    Survial_ingreso=carretera('Survial','10. SURVIAL','C) Ingresos',nombres_ingreso)

    Total_carretera=pd.concat([Canchaque_unidad,Canchaque_eje,Canchaque_ingreso,Norvial_unidad,Norvial_eje,Norvial_ingreso,Survial_unidad,Survial_eje,Survial_ingreso],ignore_index=True)

    #Ferrovias:
    Ind_Contr=ferrovias('Línea 1','I. INDICADORES CONTRACTUALES')
    Ind_Kil=ferrovias('Línea 1','II. KILOMETRAJE')
    Ind_Tra=ferrovias('Línea 1','III. TRÁFICO DE PASAJEROS')
    Ind_Rec=ferrovias('Línea 1','IV. RECLAMOS E INCIDENTES')
    Ind_Ing=ferrovias('Línea 1','VII. INGRESOS (NUEVOS SOLES INCLUIDO IGV)')

    Total_ferrovia=pd.concat([Ind_Contr,Ind_Kil,Ind_Tra,Ind_Rec,Ind_Ing],ignore_index=True)

    #Juntamos ambos
    Junto=pd.concat([Total_carretera,Total_ferrovia])
    Junto.insert(0,"Key1",Junto["Empresa"]+Junto["Categoría"]+Junto["Tipo"])

 #   fecha_col = "01" + "/" + meses_n[meses_.index(mes_)] + "/" + anio
  #  fecha_col = datetime.datetime.strptime(fecha_col, '%d/%m/%Y')
    fecha_col = "01" + "/" + meses_n[meses_.index(mes)] + "/" + anio
    fecha_col_dt = datetime.datetime.strptime(fecha_col, '%d/%m/%Y')
    fecha_col_str = fecha_col_dt.strftime('%d/%m/%Y')

#    Junto.columns.values[4]=fecha_col
    cols = list(Junto.columns)
    cols[4] = fecha_col_str
    Junto.columns = cols


    #Base total
    Ruta_Base=r"Z:\Mesa de Inversiones\Bottom-Up\1 Proceso de Inversión Renta Variable\Proyecto Inputs Macro\Complementos BD\Construcción\Históricos\Ositran.xlsx"
    DF_F = Junto.copy()


    with pd.ExcelFile(Ruta_Base) as writer:
        BD_Total = writer.parse(sheet_name='Sheet1')


    BD_Total_=BD_Total.copy()
    BD_Total_['Indicador'] = 'Mapeado'
    BD_Total_ = BD_Total_.set_index("Key1")
    dic_Total_ = BD_Total_.to_dict()

    DF_Total2 = DF_F.copy()
    DF_Total2['Indicador'] =  DF_Total2['Key1'].map(dic_Total_['Indicador'])

    DF_Total2 = DF_Total2.loc[DF_Total2['Indicador'].isin([np.nan])].sort_index(ignore_index=True) 
    DF_Total2 = DF_Total2.drop(['Indicador'], axis=1)
    BD_Total = pd.concat([BD_Total, DF_Total2])

    DF_Total = DF_F.set_index("Key1")
    dic_cr = DF_Total.to_dict()
 #   fecha_col = '01' +'/'+ meses_n[meses_.index(mes_)] +'/'+ anio
  #  fecha_col = datetime.datetime.strptime(fecha_col, '%d/%m/%Y')
   # BD_Total[fecha_col] = BD_Total['Key1'].map(dic_cr[fecha_col])



    BD_Total[fecha_col_str] = BD_Total['Key1'].map(dic_cr[fecha_col_str])


    with pd.ExcelWriter(Ruta_Base ,  mode="a", engine="openpyxl",if_sheet_exists = 'replace') as writer:
        BD_Total.to_excel(writer, sheet_name="Sheet1", index = False)
#%%
import warnings
warnings.filterwarnings("ignore")
#%% md
<span id="papermill-error-cell" style="color:red; font-family:Helvetica Neue, Helvetica, Arial, sans-serif; font-size:2em;">Execution using papermill encountered an exception here and stopped:</span>
#%%
for i in array_rutas[::1]:
    m = i[0:2]
    mes = meses_n[int(m)-1]
    anio = i[2::] 
    mes_ = meses_[int(m)-1]
    mes_cod=meses_cod[int(m)-1]
    mes_cod2=meses_cod2[int(m)-1]

    print(mes + ' ' + anio)
    main(mes, anio)
#%%
